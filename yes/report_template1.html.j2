<!DOCTYPE html>
<html>
<head>
  <title>Command Outputs Report</title>
  <style>
    .output-box {
      border: 1px solid #ddd;
      padding: 10px;
      margin-bottom: 20px;
    }
    .success {
      color: green;
    }
    .failure {
      color: red;
    }
  </style>
</head>
<body>
  <h1>Command Outputs Report</h1>

  {% for result in command_outputs %}
    <div class="output-box {% if result.rc == 0 %}success{% else %}failure{% endif %}">
      <h2>{{ command_outputs.item }}</h2>
      {% if command_outputs.rc == 0 %}
        <pre>{{ command_outputs.stdout | default('Command produced no output') }}</pre>
      {% else %}
        <pre>{{ command_outputs.stderr | default('Command failed or produced no output') }}</pre>
      {% endif %}
    </div>
  {% endfor %}

</body>
</html>
